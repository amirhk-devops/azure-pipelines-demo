# Continuous Integration Samples

This section contains multiple CI pipeline examples designed for different .NET backend repositories used across 40+ projects.

Each sample demonstrates specific stages (build, test, artifact handling, or conditional logic).

